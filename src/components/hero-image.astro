---
import { Image } from "astro:assets";
import groupImage from "../../public/group2.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={groupImage}
  widths={widths}
  sizes={sizes}
  alt="Kahp group"
/>
